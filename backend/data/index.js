const coursesData = require('./courses')
const topicsData = require('./topics')
const postsData = require('./posts')

module.exports = {
    courses: coursesData,
    topics: topicsData,
    posts: postsData
}